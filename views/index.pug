extends layout

block content
    h1 Circle Game
    i.subtitle Network Socket Programming

    form#login-form.login-form(method='post', action='http://localhost:3000/login')
        label(for='username') Username
        input#username(name="username" placeholder="Username" required=true)
        button(type='submit') Play
